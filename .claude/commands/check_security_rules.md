---
description: Check Python source files against Safety & Security rules in CLAUDE.md
---
Review all Python files in `src/wpr/` and verify they comply with every rule defined in the "Safety & Security" section of CLAUDE.md.

For each violation found, report:

- File path and line number
- The rule that is broken
- A suggested fix

If no violations are found, confirm that the code is compliant with all rules.
